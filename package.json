{
  "name": "bettergi-map",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:single": "vite --mode single",
    "build": "node scripts/prepare-layer-cache.js && vite build && node scripts/generate-tiles.js dist/tiles && node scripts/prepare-layer-cache.js --cleanup",
    "build:single": "node scripts/prepare-layer-cache.js && node scripts/generate-tiles.js tile-cache && vite build --config vite.config.single.js --mode single && node scripts/generate-tiles.js --cleanup tile-cache && node scripts/prepare-layer-cache.js --cleanup",
    "preview": "vite preview",
    "generate-tiles": "node scripts/generate-tiles.js"
  },
  "dependencies": {
    "@geoman-io/leaflet-geoman-free": "^2.17.0",
    "leaflet": "^1.9.4",
    "vue": "^3.4.29"
  },
  "devDependencies": {
    "@arco-design/web-vue": "^2.56.2",
    "@vitejs/plugin-vue": "^6.0.8",
    "sharp": "0.35.0",
    "vite": "8.0.16",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
